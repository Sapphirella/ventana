# Ventana 交接文档

> 给下一个接手这个项目的 agent（或者几个月后的自己）看的。**先读这一份，再读 `README.md`。**
>
> 最后更新：2026-09-16 · 对应提交见文末「版本记录」

---

## 0. 三十秒版本

- **是什么**：AI 虚拟角色聊天 App，静态网页 + PWA，手机为主。
- **在哪**：`ventana/`（本地工作区里的一个子目录），三个文件：`index.html`（结构）+ `styles.css`（样式）+ `app.js`（逻辑）。
  公开仓库 `Sapphirella/ventana` 里这三个文件就在**仓库根目录**（见第 16 节）。
- **叫什么**：Ventana（2026-09-12 从 Chambre 改名）。git 历史里的提交信息还写着 `chambre`，那是历史，别改。
- **怎么跑**：`python3 ventana/serve.py`（不缓存、打印手机访问地址），或双击 `Ventana启动.command`。
- **换网络后打不开？** 先 `python3 ventana/whereami.py` —— IP 会随 Wi-Fi 变，
  旧书签失效看起来像"网页挂了"。它给当前地址 + 能扫的二维码，并列出哪些端口没在监听。
- **怎么测**：起无头 Chrome（**必须 `--no-sandbox`**，另加三个 `--disable-*-background*`）后 `node ventana/tests/mobile.mjs`，319 项（约 46 秒）。
- **当前状态**：对话 + 人格 + 资料库（按需读文档）+ 记忆馆 + 会话归档都能用；配色是黑白试验版；**多角色已确定不做**。
- **接手须知**：第 13 节那两个"未完成 bug"已在 v0.22 结案（含根因与验证方式），别再按原文档的推测去查。已归档会话已收进记忆馆顶栏箱子的右侧抽屉（v0.9）；v0.11 起顶栏新增「导出当前会话为 txt」按钮，正在聊的会话也能一键倒成纯文本；v0.12 修掉手机端软键盘把系统提示词输入框压住、导致"没法输入"的 bug；v0.13 起导出按环境分叉——系统浏览器直接下载，内置 WebView（微信/企微/钉钉等）改为页面内导出面板（只读全文 + 复制按钮），并顺手把归档/导出两个抽屉移出视图层挂到 body 直属（之前嵌在记忆馆 view 里、其他视图下整层隐形）；v0.14 起顶栏「记忆馆」不再显示记忆条数（改在馆内显示"共 N 条记忆"，有新内容时顶栏亮感叹号、进馆看过就消失）；改系统提示词或增删资料库后，下一轮把两者全文随消息发 AI 一次（之后恢复默认）；>6000 字的大文档索取时按当前话题截取最多 6000 字（不再整篇读）；v0.15 把按需截取补齐到**每一条读取路径**（工具调用 `read_doc` 和文本协议 `[[读:]]` 都截取，2 万字文档实测只给 5 千多字），聊天里读取事件改成「读了《文档名》· 给了多少/总字数（按需截取）」如实呈现、`read_doc` 工具描述也改成"按需截取不会整篇输出"，并新增 **`doc-bigram-snippet.md`** 完整说明 bigram 打分逻辑；顺带把记忆馆「共 N 条记忆」字号 12→14px、与上方提示词的间距从 -4px 拉到 14px（v0.15）。v0.16（2026-09-14，另一 agent 补的 review 修复，已验收）：顶栏模型名超长时正确省略号截断、不再压到导出按钮（截断设在 `#modelTag` 文字层而非 `.chip` 容器层）；资料库**删空**也会如实告知模型「资料库现在是空的、之前的文档已全部删除」，「更新标记」改为请求**成功后**才消费（失败下轮重发）、演示模式不再记标记；重开 App 后开场白按当前连接状态刷新（不再残留"演示模式"版本）。v0.17（2026-09-14）：演示模式的内置回复整段换成作者新文案——开门见山自报「我叫 Ventana，意思是窗户，但现在只是一扇假窗」，讲清演示模式的话是预置的，然后指引接入三步（右上角齿轮 → 接口地址/API Key/模型名 → 测试连接 → 保存），再讲系统提示词与资料库的差别（提示词每轮注入、资料库按需检索最多 6000 字），结尾「在我连接上之前，就当我在后台修容吧」；顺带补上 227a19d 漏掉的 VERSION 升级（v0.16→v0.17，app.js 与 sw.js 同步）。v0.18（2026-09-14）：用户消息改为**段落化渲染**——新增 `appendPlainParas()`（纯文本版段落，不解析 `**`/反引号等行内标记），`\n\n` 拆 `<p>`（`p { margin: 0 0 10px }`，与模型回复一致的紧凑间距）、`\n` 拆 `<br>`；修复以前 `textContent` + pre-wrap 把用户插入的空行渲染成整行高空白、和模型回复段落间距对比突兀的问题，现在两边视觉一致（实测用户消息 p 间距 10px vs 开场白 8–10px）。v0.19（2026-09-14）：**手机触屏回车改为换行而不是发送**——`keydown` 按「设备敏感」分叉：触屏设备（`navigator.maxTouchPoints > 0`，手机软键盘）回车=插入换行、发送交给按钮，桌面键盘 Enter 发送、Shift+Enter 换行；判定只信 `maxTouchPoints`（Chrome 桌面 `'ontouchstart' in window` 恒为真，拿它当判据会把台式机误认成触屏）、textarea 加 `enterkeyhint="enter"` 让软键盘回车键显示成「换行」。**AI 内置回复（演示模式的假窗回复 + 开场白）间距收紧**——两类预置文本的气泡加 `.builtin` 标记，行距 1.75→1.62、非标题段落间距 10→6px（标题 `.hello` 保持 8px），用户气泡（1.62/10px）与真实 API 回复（1.75/10px）排版原样不动；验证：干净 Chrome 实测开场白行距 25.11px、段落 6px，用户气泡多段实际间距 10px、真实回复行距 27.125px 均未受影响，全量测试 288→309 项全绿（新增 21 条：触屏回车不发送且插入换行、桌面回车发送、Shift+Enter 不发送、builtin 标记、三档间距互不污染）。**v0.20（2026-09-14）**：作者反馈"内置回复空行间距改回最早那版"——`.builtin` 收紧整块回退：删掉 `runDemo()` 与开场白的两处 `classList.add('builtin')` 和 `styles.css` 里 `.reply.builtin` 三条规则，内置回复回到与真实 API 回复同款排版（行距 1.75 / 段距 10px），用户气泡（1.62 / 10px）始终不变；同时按作者新原文重写 `DEMO_REPLY` 开头（"你好，我是 **Ventana** 。它的意思是「窗户」，但我现在只是一扇假窗。演示模式下，我说的话都是预置的，不是真的在回复。"），接入三步、系统提示词/资料库 6000 字检索说明、"后台修容"结尾四段内容不变，仍拆 4 条连发保持流式节奏；验证：干净 Chrome 全量测试 309→319 项全绿（新增 10 条：开场白/演示回复不再带 builtin 且行距 27.125px、段落 10px/单段归零、8 个新文案关键段落含粗体不渲染星号逐条命中——`**Ventana**` 在 textContent 里是「Ventana」、彩蛋句原文是"**我**在后台修容"不是"你"）；VERSION 升 v0.20（app.js 与 sw.js 同步）；README/HANDOFF 已同步。
- **最要紧的规矩**：改完代码**同步改 `README.md` 和这份文档**，然后 git 提交；
  每次提交后**立即**运行全量测试（319 项基线），全绿再推送。

---

## 13. 两个 bug 已修（原「未完成」，2026-09-15 接手后結案）

### Bug A：气泡之间的「大空行」—— **根因是布局，不是缓存** ✅ 已修

原交接文档猜"用户跑的是旧代码（SW 缓存没更新）"，**猜错了**。真根因：

`.row .col` 上有一条**无条件**的 `padding-bottom: 38px` —— 它是给气泡下面那排
操作图标预留高度的。但一轮回复被拆成多条气泡时，图标**只挂在最后一条**下面，
于是前面每一条都白留 38px。

实测（演示模式一轮 6 条气泡，只有 2 条有图标）：

| 行 | 有图标 | 白留空白 |
|---|---|---|
| 1 / 3 / 4 / 5 | ✗ | 各 38px |
| 2 / 6 | ✓ | — |

**合计 152px ≈ 5.2 行文字高**（单行文字才 29px），而且它**随气泡条数累积**，
越聊越明显。用户看到的"占三行的大空行"就是它。

修法：`padding-bottom` 改成按需 —— `attachActions()` 挂上图标排时给 `.col`
加 `has-acts` 类，CSS 里只有 `.row .col.has-acts` 才预留。
修完实测：没有图标的行预留 **0px**，气泡间距回到 `18px`（有图标的那条 56px）。

> **怎么确认这个诊断是对的**：改之前把 4 条气泡的行高与文字高相减，得到
> 38/38/38/38 的白留；改之后是 0/38/0/0/0/38。回归测试见
> `mobile.mjs` 的「气泡之间的空行」一组，**把修复还原后它会立刻失败**（已验证）。

顺带记一条方法论教训：原文档说"不要再用无头 Chrome 模拟，要连真实设备"——
这次恰恰是**无头 Chrome 里量 DOM 几何**定位到的（像素截图只能看到现象，
量 `getBoundingClientRect()` 才能看到 38px 这个数）。两者配合才对：
截图用来确认"用户看到了什么"，DOM 量测用来定位"是谁留的空白"。

### Bug B：点模型名进去又被弹回 —— **找到了机制，做了防御** ✅ 已加固

原文档列的四个推测（键盘 resize / hash 路由 / 事件冒泡 / blur）**都不是**。
真机制最可能是这个：

**Service Worker 自动刷新打断了用户操作。** `sw.js` 换版本后，新旧 worker
交接（`controllerchange`）通常发生在**打开页面后 0.5~3 秒内**，而原代码一到就
无条件 `location.reload()`。用户正好在这时点顶栏模型名 → 设置页刚打开，
页面在他眼前被刷掉、回到对话区 —— 表现就是"点一下进去了又被弹回来，
再点一次才停得住"（因为刷新只发生一次）。

修法：给自动刷新加**交互保护** —— 监听 `pointerdown / touchstart / keydown`，
5 秒内有交互就先推迟，等用户静置 5 秒再刷（是推迟，不是取消）。

同一轮里还修掉两个"设置页打开着"时会犯的真 bug：

1. **`--composer-h` 被写成 0px**：切到设置页时 `#viewChat` 是 `display:none`，
   `composerWrap` 量出来高度是 0，原代码老老实实写进 CSS 变量 ——
   回到聊天后消息区底部避让消失，最后一两句会被固定输入卡压住。
   （截图里确实能看到最后一行被输入框压住 13px。）
2. **设置页继承上次的滚动位置**：重新打开时可能停在中间，看起来像"没打开"。

> ⚠️ 仍未能**亲自复现** Bug B 的现象：在无头 Chrome 里试过触摸点按、鼠标点击、
> 键盘弹起（缩小视口模拟）、横竖屏、滚动后点按、多种宽度 —— 都是一次点开、
> 稳定停留。如果加固之后用户还能复现，请他开 DevTools 录一次事件时序，
> 重点看：点击后 1 秒内有没有 `resize` / `visibilitychange` / 页面重新加载。

## 14. 本轮的教训（写给自己或下一任）

1. **不要用无头 Chrome 代替真实设备做视觉验证**。无头 Chrome 的 flex gap、line-height、默认 margin 都和真实浏览器有微妙差异。如果用户说「看起来不对」，先连真机 DevTools，不要反复改代码猜。
2. **SW 缓存会让所有改动看起来「没生效」**。v0.21 的修改理论上是对的，但如果用户没清缓存，他看到的仍然是旧代码。每次改完应该**先清缓存再验证**，而不是改完就跑测试。
3. **「空行」这个词有两个完全不同的含义**：
   - 源码里的 `\n`（渲染为 `<br>`，行内换行）
   - 源码里的 `\n\n`（渲染为 `<p>`，段落间距）
   用户说「大空行」时，要先问清楚是「气泡内部句与句之间」还是「气泡与气泡之间」，不要混为一谈。
4. **先定位根因，再改代码**。本轮花了太多时间在「改文案 → 改 CSS → 改 join → 升版本」上，但没有一次真正确认过用户浏览器里的 DOM 结构。如果一开始就要求用户开 DevTools dump DOM，可能一轮就能定位。
5. **两个 bug 不要混在一起修**。用户一开始只说了空行，后来又加了弹回 bug。应该先修完一个、验证完一个，再开下一个。混在一起会导致测试变复杂、回滚困难。

---

## 1. 这个工程的历史（不知道会踩雷）

这个工作区里跑过**两版** Ventana，别搞混：

| 版本 | 位置 | 状态 |
|---|---|---|
| **v1 六页版** | `../废案/chambre-v1-260912/`（也曾在这个目录里，git 历史 `93ff9c4` 之前） | **已废弃**，但代码完整，零件可以抄 |
| **v2 PWA**（当前） | `ventana/`（本地）/ 仓库根（公开仓库） | 在用 |

v1 是六个页面（消息 / 聊天 / 朋友圈 / 通讯录 / 我的）、
`css/` 六个文件 + `js/` 十五个文件共 7759 行、五个 Chrome 冒烟测试脚本。
它被作者主动放弃，原因是维护成本太高、方向调整成"先做一个能用的单页 PWA"。
**它不是垃圾**：`parser.js`（【说】【想】【隐】【静】协议解析）、
`pacing.js`（打字节奏引擎）、`sentence.js`（智能断句）这三块是纯逻辑、
有单测、可以整块搬进新架构。搬的时候注意 v1 依赖它的目录结构（ES Module 导入路径）。

git 历史里还留着 v1 的全部代码（`git show 93ff9c4:ventana/css/chat.css` 就能取回），
所以**不要因为"找不到旧实现"而重写**。

### 关于这个 git 仓库

- 本地工作区的仓库根**不是** Ventana —— 除 `ventana/` 外还有若干私人素材目录。
  **公开仓库只包含 `ventana/` 里的内容**（见第 16 节），私人素材一律不发布。
- 提交信息用 **Conventional Commits + 中文正文**：`fix(ventana): 一句话说清改了什么`，
  正文写清「为什么这么改 / 怎么验证的 / 踩了什么坑」。历史提交（`git log`）都是这个格式，
  照着写就对了。
- **`废案/` 和 `参考文件-*/` 不在仓库里**（参考文件体积大且是第三方），
  只在文档里引用路径。如果哪天要归档，记得它们需要单独处理。

---

## 2. 代码地图

**先看结构**：`index.html`（140 行，只有结构）/ `styles.css`（380 行，只有样式）/
`app.js`（约 1900 行，全部逻辑）。加功能前**先确认该放哪一层** ——
不要再把逻辑写回 HTML 的 `<script>` 里。

### app.js 的段落顺序就是它的数据流

| 区段 | 内容 | 关键点 |
|---|---|---|
| 键名常量 + `migrateKeys` | localStorage 的七个键 | ★ 见第 10 节，别删迁移 |
| `readJSON/writeJSON/uid` | 存储与配额 | 写失败会 toast 提示，不静默丢数据 |
| **会话 store** | `store.convs` / `current` / `msgs()` | 一个会话对象持有 `messages`；归档只是打标记 |
| **资料库 docs** | `loadDocs/addDoc/findDoc/docIndexText` | 只存本地；`docIndexText` 才是进提示词的那份索引 |
| **记忆馆 memories** | `loadMemories/addMemory/memoryIndexText` | AI 写、用户删；索引进提示词，正文按需取 |
| 配置与人格 | `loadCfg/loadPersona/buildMessages` | ★ `buildMessages` = 人格 + 文档索引 + 记忆索引 |
| 消息区渲染 | `addMsg/makeRow/addSystem/restoreLog/attachActions` | 行结构 `.row > .col > (.body + .acts)`，见第 11.5 节 |
| 消息菜单 | `actIcon/actionMenu/doCopy/doDelete/doRegen` | 图标是内联 SVG；直接改存储，不是只改界面 |
| 滚动 / 输入卡 | `syncComposer/scrollFollow` | `--composer-h` 由 JS 实测写入 |
| 流式渲染 | `paintSoon/appendDelta/finalize` | ★ rAF **加**兜底定时器，见第 9 节 |
| **流式请求** | `streamChat` | 带 tools；参数是**分片**流进来的，必须按 index 拼 |
| **文本协议** | `MARK_RE/runAction/runToolCalls` | `[[读/记/忆]]` 是 tools 的退路 |
| **发送流程** | `send/runDemo/runApi` | ★ `runApi` 是**多轮循环**，见第 12 节 |
| 配置页 / 记忆馆 / 归档 | `fillCfgForm/renderMemory/renderArchives` | |
| 启动 | 文件末尾 | 顺序：`loadStore → loadDocs → loadMemories → loadPersona` |

### 四个最容易改错的地方

1. **停止（`stopChat`）**：一轮回复可能是**多条气泡**（工具调用会把它打断）。
   要撤的是"还没吐出字的那条"，不是数组最后一条 —— 见第 6 节。
2. **落盘（`pushMsg`/`commitReply`）**：先落一条空的 assistant 记录，每次定稿往里补，
   否则用户中途按停止的那条刷新后就没了。
3. **`doRegen`**：要先把旧回复的 DOM 节点拆掉再 `beginReply()`。
   不拆的话 `currentAiMsg` 指向的对象已经从数组里 splice 掉了，
   新老记录会互相盖（看起来像"重新生成没生效"）。
4. **菜单挂载（`attachActions`）必须幂等**：重新渲染会走同一条路径，
   不判断的话菜单叠成两层，按钮数会翻倍。

## 3. 测试基建（改代码前先跑一遍）

见 README「测试」一节。这里只记**为什么这么搭**和**坑在哪**：

- `cdp.mjs`：零依赖 CDP 客户端（Node 内置 `fetch` + `WebSocket`）。
  起 Chrome 必须 `--no-sandbox`，否则 macOS 沙箱下直接崩。
- `serve.py`：开发服务。**别用 `python3 -m http.server`**——它不加缓存头，
  配合浏览器缓存和 Service Worker，手机上会反复出现"改了代码看不到"；
  它也不打印局域网地址，手机上不好开。
- `png.mjs`：自己写的 PNG 解码器。**这是本项目的核心测试资产**——
  它让"画出来是什么样"变成可断言的东西。扩展测试时优先用它，别退回读 CSS 声明。
- `mobile.mjs`：319 项（约 46 秒），按主题分组：气泡形态 / 布局与安全区 / 交互流程 / 横向溢出与设置页 /
  黑白约束 / 品牌与数据迁移 / 系统提示词 / 请求体里的 system 消息 / 版本自愈 /
  演示模式自动兜底 / rAF 失效兜底 / 资料库按需读取 / 气泡菜单 / 顶栏导出当前会话 / 会话归档 / 记忆馆 /
  顶栏文字截断 / 全量同步的边界（v0.16 加）/ 触屏回车换行与桌面回车发送（v0.19 加）/ 内置回复与真实回复同款排版（v0.20 改，含新文案关键段落断言）。
  组内断言的具体判据都写在文件注释里。

### 写测试的四个坑（都真踩过）

1. **旧 Service Worker 会让所有改动看起来"没生效"。** `sw.js` 已改成导航请求网络优先，
   并且 `index.html` 用 `sw.js?v=<VERSION>` 注册、`controllerchange` 时自动刷新一次。
   如果改动还是刷不出来：先看 DevTools → Application → Service Workers，
   再确认 `index.html` 的 `VERSION` 和 `sw.js` 的 `VERSION` 是同一个值（改版本时要同步改两处）。
2. **静态服务必须起在 `ventana/` 这一层。** 本机另一个工程里也有同名目录，
   起错会看到另一个 App，而现象是"我的改动毫无效果"。
3. **别轮询瞬时状态**（几十毫秒的 DOM 状态永远抓不稳），改成验证确定性不变量。
4. **`Page.addScriptToEvaluateOnNewDocument` 每次都执行**，用完要移除。

---

## 4. 被报告的 bug 与修复记录

### 夜间模式下移动端的对话框是"纯方块"，不是气泡（2026-09-12 已修）

- **现象**：手机夜间模式下，对方的回复是一整块通栏方板，不像气泡。
- **根因**：对方回复被套了 `.bubble`（灰底 `--bg-2` + 1px 描边 + 圆角）。
  宽度接近满屏（390px 视口下列宽 308px），夜间是 #252525 铺在 #0f0f0f 上——
  视觉上就是一整块方板；浅色情况下底/边对比更明显，反而更像"卡片"。
  真正的问题不是圆角没生效，而是**对方回复根本不该有盒子**。
- **修法**：对方回复改成纯排版（`.row > .body.reply`，无背景 / 无边框 / 无圆角），
  只有我方消息保留气泡（`.body.bubble`，且改用带描边的浅底而非纯反色，
  入夜后才有层次）。这与参考实现 dwell 的做法一致（它只有 `.bubble` 一种气泡类，
  对方的话是裸文字 `.gu`）。
- **验证**：`tests/mobile.mjs` —— 计算样式断言（无底色/无边框/无圆角盒子）
  + 像素采样断言（对方文本区域与背景无差异；我方气泡左上角确实被切圆）。

同时修掉的三个连带问题：

- 代码块以前硬编码 `#0c0c0c`，在**日间**是一块黑砖 → 改成跟随主题的
  `--code-bg/--code-line/--code-text`。
- 旧 `sw.js` 缓存优先 → 改了代码刷新不生效，改成导航请求网络优先。
- 顶栏的 ⚙ emoji → 换成内联 SVG（emoji 在部分安卓上渲染成彩色方块，破坏黑白约定）。

---

## 5. 下一步怎么接（建议顺序）

1. 先跑一遍测试确认基建好的：起服务 + 起 Chrome + `node ventana/tests/mobile.mjs`。
2. 读 `README.md` 的「界面约定」，再动 `index.html`。
3. 按 README「下一步」的优先级做，**每完成一项就更新这两份文档并提交**。
4. 需要"抄零件"时优先去 `../废案/chambre-v1-260912/`，
   别重新造轮子（注意那边是模块化目录结构，路径要改）。

### 不建议做的事

- ❌ 为了"整洁"把 `index.html` 拆成一堆文件。当前体量（1200 行）单文件是净收益。
  真要拆的时机：① 加第二个界面（会话列表 / 朋友圈）；② 按第 8 节的方案抽成
  Web Component 准备嵌入宿主。**拆的时候用 git 历史做对照。**
- ❌ 把界面彩色化而不同步改测试里的黑白断言（会直接失败）。
- ❌ 在 PWA 里依赖 `file://` 能跑通的东西（service worker 只在 http(s) 下生效）。
- ❌ 直接把 API Key 写进代码或提交进仓库。
- ❌ 删掉 `migrateKeys()`（第 7 节）：它是一次性搬迁，但删了就等于删掉用户已配好的设置。
- ❌ 把"只有一份人格"改成"多份自动拼装"而不先问作者 —— 这是产品决策，不是技术优化。

---

## 6. 版本记录

> 用**提交信息**而不是哈希来记 —— 哈希会随 rebase / amend 变，
> 上一轮就在这里绕了两圈（写了哈希 → amend → 哈希变了 → 再补一次提交）。
> 要看具体改动：`git log --oneline` 按信息找，或 `git log -p -- ventana/`。

| 提交信息 | 内容 |
|---|---|
| `chore(chambre): 重置为 PWA 单文件基线 —— 旧六页版本已归档到 废案/` | v1 六页版归档；确立单文件 PWA 架构；新增 CDP 测试基建 |
| `fix(chambre): 修复夜间模式「方块」气泡 + 移动端对话界面重做` | **被报告的 bug**：对方回复去掉盒子（只有我方是气泡）；移动端布局重做（输入卡悬浮层、时间戳、正在输入、回到底部、多条连发）；修掉停止不落盘 / 删错节点 / 空气泡三个 bug；Service Worker 改网络优先；新建 README 与本交接文档；测试 62 项全过 |
| `docs(chambre): 交接文档里的版本号改为定稿后的提交哈希` | 文档修正 |
| `chore(chambre): 开发体验收尾 —— 不缓存的 serve.py、版本化 Service Worker、启动器接上` | 新增 `serve.py`；`index.html` 用 `sw.js?v=<VERSION>` 注册并在 `controllerchange` 时自动刷新；启动器改用 serve.py；删掉无引用的 `.brand/.logo` |
| `refactor(ventana): 改名 Chambre → Ventana` + 同批次的 `feat(ventana): 系统提示词…` | 目录、启动器、manifest、SW 缓存名、文案全部改名；localStorage 加 `chambre.*` → `ventana.*` 迁移；设置页新增「系统提示词 · 决定它是谁」（textarea + 上传 .md/.txt/.json，整段替换）+ 独立的「保存人格」；`buildMessages()` 负责把人格作为 system 消息发出去；测试 92 项（新增迁移、人格、请求体三组） |
| `fix(ventana): Service Worker 旧缓存自愈 + VERSION 升到 v0.4` | 缓存命中时 SW 发 `stale-page` 通知、页面收到自动刷一次（每次加载只刷一次，防循环）；`sw.js` 加 `stale-page-selftest` 钩子让这条真实通道可测（页面自己 dispatchEvent 打不到 SW 监听器）；改 sw.js 必须升 VERSION，否则浏览器 24h 内不会重取；测试 122 项 |
| `feat(ventana): 演示模式改成自动兜底（去掉模式滑块）+ 系统提示词独立成块` | 删掉「连接方式」滑块与 `cfg.demo` 字段，改为按三项是否填完判定；加状态横幅与「清除连接」；两块之间加间距与分隔线；顺手修掉无头环境 rAF 不触发导致"字不显示"的问题（`paintSoon` 兜底）；测试 122 项 |
| `refactor(ventana): 单文件拆成 index.html + styles.css + app.js` | 为接下来的四个大功能腾出可读性；SW 预缓存补上新文件（预缓存列表变了必须升 VERSION） |
| `style(ventana): 消息操作改成气泡下方的小图标排` | 文字按钮 → 内联 SVG 图标；从气泡左缘开始排（不再靠右）；点击区 36×36；修掉 `.acts` 被当成横排第二列挤成 38px 的结构问题；测试 205 项 |
| `feat(ventana): 开场白保留 + 操作图标贴边、横向一排` | 开场白接上 API 后也保留（文案按作者给的原文），并**不进模型历史**、**不挂图标**；我方图标贴气泡右下角、对方图标贴整段消息左下角、永远横向；修掉「图标挂在一轮回答的第一条气泡下面（夹在两段话中间）」和「点停止的回答没有图标」两个 bug；演示节奏加测试用快进开关（套件 55s → 46s）；测试 219 项 |
| `feat(ventana): 资料库 / 气泡菜单 / 会话归档 / 记忆馆 四个功能` | 资料库按需读取（索引进提示词、正文 tools 或 `[[读:]]` 索取）；每条气泡下 复制/重新生成/删除；会话归档（打标记 + 导出 txt + 取消归档）；记忆馆由 AI 读写摘要；修掉 showTyping 不显示、记忆不重排、归档删除不刷新、重新生成留空壳四个 bug；测试 186 项 |
| `feat(ventana): 已归档会话收进记忆馆箱子的侧边抽屉 + 资料库删除按钮图标化` | 归档列表从记忆馆主体迁进顶栏箱子弹出的右侧抽屉（遮罩/关闭钮/Esc 可关，归档后抽屉数据保持同步）；记忆馆主体只留记忆，不再有「已归档会话」分区；资料库文档删除按钮从突兀的文字改为垃圾桶图标（36×36 触摸目标，与聊天图标同一规格）；VERSION 升 v0.9；测试 228 项全绿 + 11 项 UI 目检通过 |
| `fix(ventana): 去掉归档抽屉的「取消归档」按钮 + 测试不再往下载目录写文件` | 归档会话只保留 导出/删除/改名，删掉「取消归档」入口（用户要求，归档后的会话统一留在抽屉里管理）；顺带修掉 mobile.mjs 导出测试的真实下载副作用（之前跑一次全量测试就往 `~/Downloads` 落一个"第一个会话*.txt"，累计 24 个）；VERSION 升 v0.10；测试 228 项 |
| `feat(ventana): 顶栏新增导出当前会话为 txt 的按钮` | 顶栏加下载图标按钮（与 💬/⚙ 同款 iconbtn 黑白规格），点击把当前活跃会话经 `convToText` 倒成 `标题.txt` 下载，空会话 toast 提示；测试补「顶栏导出当前会话」一组 5 条断言（含把真实的 `a[download]` 点击摁掉，防止再往 `~/Downloads` 落文件）；VERSION 升 v0.11；测试 233 项全绿 |
| `fix(ventana): 手机端软键盘不再压住系统提示词输入框` | 手机点 textarea 弹键盘后可视高度 844→480，`.cfg` 是嵌套滚动容器，浏览器只做最小滚动——输入框下半截被键盘压住、光标看不见，表现是"没法输入系统提示词"（iOS 布局视口不缩，只有 `visualViewport` 知道真相）。修法：聚焦时 + `visualViewport` resize 时把输入框滚到键盘上方可视区中央；测试补「键盘弹起后输入框可见可输」3 条断言（CDP 把视口压到 480 模拟键盘，断言输入框整体在可视区内 + 还能真的敲进字）；VERSION 升 v0.12；测试 236 项全绿 |
| `fix(ventana): 内置 WebView 里导出会话不再白屏 —— 改为页面内导出面板 + 抽屉移出视图层` | 手机端导出 txt 的根因两条：①内置 WebView（微信/企微/钉钉/支付宝等）里 `a[download]` 不可靠——点击会把 `blob:` URL 当导航打开，跳转后 blob 只在来源页面有效，直接白屏「页面出不来」；②`#archDrawer` 与 `#exportSheet` 原本嵌在记忆馆 `<main>` 里，聊天/配置视图下该 main 是 `display:none`，整个 fixed 抽屉渲染为零（DOM 有 open 类也看不见）。修法：`app.js` 新增 `isEmbeddedWebView()`（UA 名单 + `'download' in a` 能力兜底），`download()` 分叉——系统浏览器走原 blob 下载，WebView 走页面内导出面板（只读 textarea 全文 +「复制全文」按钮，Esc/遮罩/关闭钮可关）；两个抽屉移出 view 层挂到 body 直属。测试补 8 条断言（微信 UA 注入后不再走 blob+a[download]、面板打开、含全文、复制可用、可关闭）；VERSION 升 v0.13；测试 244 项全绿 |
| `feat(ventana): 记忆馆顶栏不再显示条数 + 新内容亮感叹号 + 人格/资料库更新后首轮全量下发 + 大文档按需截取` | 三条新需求一起上：①顶栏「记忆馆」不再渲染记忆条数，改在馆内显示「共 N 条记忆」（新增 `#memTotal`）；AI 每写入一条新记忆就置未读标记，顶栏亮感叹号（`#memNew` 替代原 `#memCount`），进记忆馆查看即清除、返回对话后消失。②`buildMessages()` 新增同步标记 `ventana.sync`：保存系统提示词 / 上传或删除资料库文档时置位，下一轮把 prompt 全文或 docs 全部正文整段发一次（带「【系统提示词刚刚更新】/【资料库刚刚更新】」声明），消费后恢复默认。③>6000 字的大文档索取时不再整篇给：`docSnippet()` 用字符 bigram 与最近聊天话题打分，从命中块向两侧扩展≤6000 字的连续区间；`splitDocBlocks()` 修掉 `slice(0,1200)` 丢字（超长无标点段切成连续块）。坑：扩展循环“取较短侧”若一侧 blocked 会死循环、块集合拼接会夹带文档尾部，均改为“取较短侧装不下即整体停止”的连续窗口；上传文档后首轮全量下发让旧断言“system 没被撑大”翻转，测试改为「首轮全量 → 次轮恢复索引」。测试 244→265 项全绿 + 3 张 UI 目检（感叹号亮 / 馆内共 3 条 / 返回后消失）；VERSION 升 v0.14 |
| `feat(ventana): 大文档按需截取补齐到所有读取路径 + bigram 逻辑成文 + 记忆馆总数行样式` | 用户反馈"AI 平时还是把资料库两万字整篇读"——排查确认代码层日常读取路径（`read_doc` 工具 + `[[读:]]` 文本协议）早已走 `docSnippet` 截取，误导点是两处：①读取时系统事件只显示文档**总字数**（"读了《x》（20000 字）"），让用户以为整篇被读；②`read_doc` 工具描述声称"读取全文"，模型预期错配。v0.15 修法：读取事件改为「读了《x》· 实际给出/总字数（按需截取）」如实呈现；`read_doc` 描述改为"长文档按需截取、最多 6000 字、不会整篇输出，读不完整是正常的"；`docSnippet()` 返回值从字符串改为 { text, matched }，无命中时如实告诉模型"没找到相关部分，先给开头"。新增大文档按场景测试：2 万字文档经**工具调用路径**仅给 5622 字（含目标话题、无尾部内容、事件显示 5564/20650）。另写 `doc-bigram-snippet.md`（随工程同目录）完整记录 bigram 打分逻辑（清洗/滑窗/分段/打分/命中判定/连续区间扩展/边界行为）；记忆馆「共 N 条记忆」字号 12→14px、margin-top -4→14px（不再贴上方提示词）。测试 265→273 项全绿（含新样式断言与工具路径大文档组、把既有"空气泡"断言从瞬时检查改成轮询等渲染落定以消除偶发）+ 记忆馆 UI 目检（gap 14px）；VERSION 升 v0.15 |
| `fix(ventana): review 修两个 bug —— 顶栏模型名截断、资料库删空后同步通知丢失` | 给 `#modelTag` 单独设截断（容器上的 ellipsis 管不到子元素）；资料库删空时如实发「已清空」并改成请求成功后才清待同步标记；演示模式下不记该标记；删掉死代码 `switchTo()`；VERSION 升 v0.16；测试 273→292 项（新增 19 项回归，已验证去掉修复即失败） |
| `fix(ventana): 重开 App 后开场白变回「演示模式」版` | `refreshGreeting()` 只在保存/清除连接时调用、漏了启动路径，接上 API 后重开 App 开场白还会挂「演示模式」文案。修法：启动序列里 `restoreLog()` 之前调一次 `refreshGreeting()`。测试 292→294 项（新增 2 条：已连接版开场白仍在、无「演示模式」残留）；**此提交改了 app.js 但未升 VERSION（仍是 v0.16）** —— 静态资源是缓存优先+后台更新，用户刷新两次仍能拿到新版，不构成功能缺口，但不符合"改代码必须升 VERSION"的硬规矩，接手时可统一升 v0.17 |
| `feat(ventana): 演示模式内置回复换成"假窗"新文案 + 补升 v0.17` | 按作者给的原文整段替换 `DEMO_REPLY`：自报「Ventana 是窗户的意思但现在是假窗」→ 演示模式的话是预置的 → 接入三步（右上角齿轮、接口地址/API Key/模型名、测试连接、保存）→ 系统提示词每轮注入 vs 资料库按需检索最多 6000 字 → 结尾"后台修容"彩蛋。文案仍拆 4 条连发模拟真人停顿，流式节奏不变。顺带把 227a19d 漏升的 VERSION 补上：app.js 与 sw.js 同步 v0.16→v0.17。验证：干净 profile（删 `/tmp/ventana-chrome`）全量测试 288 项全绿——与 v0.16 基线 294 项的 6 项差异全是断言名里的动态值（4 条 VERSION v0.16→v0.17、菜单行数 5→4、文本宽度 139→354px），断言集归一化后与基线完全一致、无真实断言丢失；另写独立脚本等流式自然走完验证 4 条完整出现 + 7 个关键段落（假窗/预置/右上角齿轮/测试连接保存/系统提示词每轮注入/最多 6000 字/后台修容）全部命中 |
| `fix(ventana): 用户消息段落化渲染，插入空行不再把段距撑大` | 用户消息原来直接 `textContent` + pre-wrap：文本里 `\n\n` 会被渲染成**一整行高**的空白（约 25px），而模型回复走 `renderMarkdown` 把 `\n\n` 切成 `<p>`（`p { margin: 0 0 10px }`），两边段落间距差异明显，作者反馈"空行分得太开"。修法：新增 `appendPlainParas(el, text)`——和 AI 侧同款段落规则（`\n\n`→`<p>`、`\n`→`<br>`），但**不解析** `**`/反引号/`~~` 等行内标记，用户发什么显示什么；`addMsg('me', …)` 改走该函数。验证：干净 Chrome 实测用户消息 p 间距 [10,10,10] vs 开场白内置文本 [8,10,10,10] 一致；气泡高度 196→150px；全量测试 288 项全绿；VERSION 升 v0.18 |
| `feat(ventana): 手机触屏回车换行不发送 + AI 内置回复间距收紧` | 两条作者反馈合并处理。① 手机端无法换行：软键盘回车原来直接就是发送。修法：`keydown` 按**设备敏感**分叉——触屏设备（`navigator.maxTouchPoints > 0`，手机/平板软键盘）回车=插入换行不发送、发送交给按钮；桌面键盘 Enter 发送、Shift+Enter 换行。**判据只用 `maxTouchPoints`**：Chrome 桌面 `'ontouchstart' in window` 恒为真，若用它会把台式机也认成触屏、回车发不出消息（测试里专门关掉触摸仿真验证了桌面分支）。`index.html` textarea 加 `enterkeyhint="enter"`，软键盘回车键显示「换行」。② 手机端内置回复空行间距偏大、电脑端刚好：根因是内置回复与真实回复共用 `.reply` 的行距 1.75 / 段距 10px，手机上显得疏。修法：给内置回复气泡加 `.builtin` 标记（`runDemo()` 的新气泡 + 开场白 greet 气泡各一处），CSS 只对 `.reply.builtin` 收紧——行距 1.62、非标题段落间距 6px（`.hello` 大标题保持 8px）；用户气泡 `.bubble`（1.62/10px）与真实 API 回复 `.reply`（1.75/10px）一行不改。验证：干净 Chrome（删 `/tmp/ventana-chrome`）全量测试 288→309 项全绿——新增 21 条：触屏合成 keydown 行数不变不发送、CDP 真实按键输入框插入换行、桌面 Enter preventDefault + 清空 + 行数增加、桌面 Shift+Enter 不发送不清空、开场白/演示回复带 builtin 且行距 25.11px/段落 6px、用户气泡多段实测间距 10px 且无 builtin、真实回复行距 27.125px/段落 10px；VERSION 升 v0.19（app.js 与 sw.js 同步）；README/HANDOFF 已同步 |
| `feat(ventana): 内置回复间距回退最早版 + 换新"假窗"文案（v0.20）` | 作者反馈：内置回复空行间距改回最早那版；并把演示模式内置回复换成新文案。① **间距回退**：删掉 `runDemo()` 新气泡与开场白 greet 气泡的两处 `classList.add('builtin')`、删掉 `styles.css` 里 `.reply.builtin` 三条规则（行距 1.62→恢复与真实回复同款的 1.75、非标题段落 6px→10px），内置回复与真实 API 回复排版完全一致；用户气泡 `.bubble`（1.62/10px）一行未动。② **新文案**：重写 `DEMO_REPLY` —— 开头改为作者原文「你好，我是 **Ventana** 。它的意思是「窗户」，但我现在只是一扇假窗。演示模式下，我说的话都是预置的，不是真的在回复。」，后三段（接入三步 → 系统提示词/资料库差别与最多 6000 字检索 → "后台修容"结尾）内容不变，仍拆 4 条连发保留流式停顿节奏。验证：干净 Chrome（删 `/tmp/ventana-chrome`）全量测试 309→319 项全绿——新增 10 条：开场白/演示回复无 builtin 且行距恢复 27.125px、段落 10px（单段消息收尾归零）、8 个新文案关键段落逐条命中（含两个断言陷阱：`**Ventana**` 渲染成粗体后 textContent 无星号，按「你好，我是 Ventana」断言；彩蛋句原文是"**我**在后台修容吧"不是"你"，首次跑挂了两条、修正断言后全绿）；VERSION 升 v0.20（app.js 与 sw.js 同步）；README/HANDOFF 已同步 |
| `fix(ventana): 演示回复历史恢复不再出现大空行（v0.21）` | 作者反馈演示回复刷新后句与句之间有大空行。根因：演示模式 4 条气泡被 `commitReply` 用 `\n\n` 拼接写入 localStorage，`restoreLog` 恢复时渲染成 4 个独立 `<p>` 段落（段距 10px），视觉上就是大空行。修法：`commitReply` join 分隔符 `\n\n` → `\n`，刷新后渲染为单 `<p>` 内多个 `<br>`，与实时流式同款排版；开场白多段落结构不受影响。验证：全量测试 319 项全绿；CDP 探针确认落盘内容 `\n\n` 出现次数为 0，刷新后 `.reply` 渲染 nP=1 / br=7 / pMargin=0px；VERSION 升 v0.21 |
| `fix(ventana): 跨浏览器兼容 —— 夸克气泡空白 + OPPO 顶栏空白/压矮 + 改完拿不到新 CSS` | `.row .col` 去掉 flex-grow（旧内核把容器高度算成整行高 → 气泡下方一大片空白）；顶栏 min-height 改为含 padding 的 `calc(var(--sat)+46px)`，`env(safe-area-inset-top)` 封顶 30px 且带回退值，另加 `applySafeArea()` 用 visualViewport.offsetTop 实测纠正重复计算；SW 把 html/css/js 改成网络优先、引用加 `?v=`（serve.py 自动填 VERSION）；VERSION 升 v0.25；测试 354 项 |
| `feat(ventana): 图标 C→V + 版本号 v0.24` | 将 PWA 图标从字母 C 替换为 V（黑色圆角背景 #111111 + 白色粗体无衬线字母），本地 Pillow 脚本生成 icons/icon-192.png 与 icons/icon-512.png；同步更新 app.js 与 sw.js 的 VERSION 至 v0.24，确保 SW 预缓存刷新；全量测试 344 项全绿 |

---

## 7. 改名叫 Ventana 做了什么（2026-09-12）

改名本身是机械操作，但有两件**不能漏**的事：

1. **localStorage 的键**。旧键是 `chambre.cfg` / `chambre.msgs`，
   新键是 `ventana.*`。`index.html` 里的 `migrateKeys()` 在读取之前把旧键搬过去
   （只在新键为空时搬，不覆盖）。**这段代码不要删**，删了的话已经配好 API Key 的人
   会看到"设置全没了"。测试里有断言守着（`mobile.mjs` 的「品牌与旧数据迁移」一组）。
2. **Service Worker 的缓存名**。`sw.js` 里是 `'ventana-' + VERSION`。
   改版本号时 `index.html` 的 `VERSION` 和 `sw.js` 的 `VERSION` 要一起改。

改了名但**故意没改**的地方：git 历史里的提交信息（`chore(chambre): …`）、
`废案/chambre-v1-260912/` 这个真实存在的归档目录名。
`mobile.mjs` 里有一条断言在数"源码里还剩几行提到旧名字"，只允许迁移注释那几行。

---

## 8. 关于"并进另一个项目"（作者的计划，尚未实施）

作者的计划：Ventana 以后会成为**另一个博客式系统的一部分**，那个系统要"和 AI 一起
读我的人生"。这一节是给未来做这件事的人（或未来的我）的可行性笔记。
**现在没有动任何代码**，只是把结论记下来。

### 技术上完全可行，但要先定三件事

1. **放在哪个域名** —— `localStorage` 按 origin 隔离。如果 Ventana 挂在
   `blog.example.com/ventana/`，那 API Key 和人格就存在这个 origin 下；
   换个子域就要重配。宿主是静态托管的话，这是最省事的路径。
2. **API Key 放前端还是后端** —— 现在的实现是**纯前端直连**，Key 存
   `localStorage`。个人自用没问题；**如果博客是公开的**，把 Key 交到前端就等于
   把它交给任何打开 DevTools 的人。公开场合建议把调用挪到宿主后端，前端只请求自家接口。
   → 改造成本很低：`streamChat()` 只依赖"一个返回 SSE 的 URL"，
   把 Base URL 指向自家后端、Key 交给后端即可，其余逻辑一行不用动。
3. **宿主怎么把文章内容交给它** —— 这是真正要设计的地方，见下。

### "一起读人生"的三个真实约束

| 约束 | 说明 | 应对 |
|---|---|---|
| **上下文窗口** | 几十年的日记塞不进任何窗口 | 按需检索（RAG，或"让模型自己决定要看哪几篇"），不要一次全发 |
| **隐私** | 日记是最私密的数据，一旦发到第三方 API 就出本机了 | 本地模型 / 只在用户主动点开某篇时发那一篇 / 明确告知每次发了什么 |
| **连续性** | 每次对话都从头解释"我是谁"会毁掉陪伴感 | 长期记忆要单独存。现在的人格是一整段文本，长期记忆应该是另一个可增删的层 |

### 嵌入方式的三条路（按侵入性从低到高）

1. **iframe 嵌入** —— Ventana 挂在独立路径，宿主用 `<iframe>` 引入。
   最省事，样式与脚本完全隔离；缺点是移动端键盘、滚动、深浅色跟随要额外对齐。
2. **抽成 Web Component**（推荐）—— 把 `index.html` 里的 CSS + JS 打包成
   `ventana-chat.js`，导出一个自定义元素（如 `<ventana-chat>`）。
   宿主页面直接 `<script>` 引入，样式用 Shadow DOM 隔离。
   当前单文件结构其实很好抽：CSS 是一整块 `<style>`、JS 是一整块 `<script>`，
   **抽的时候用 git 历史做对照**。
3. **并进宿主的前端框架** —— 把逻辑改写成宿主的组件。最灵活，成本也最高；
   除非宿主已经有一套聊天 UI，否则不建议（等于重写一遍流式与滚动逻辑）。

### 数据接口建议

现在 Ventana 的存储是"两条键 + 一条 messages 数组"，对嵌入来说是好事：
宿主只需要一个能读写的**上下文提供者**接口，例如

```
Ventana.setContextProvider({
  getSystemPrompt()  -> Promise<string>     // 人格 + 长期记忆
  retrieve(query)    -> Promise<Context[]>   // 按需检索出来的文章 / 日记
})
```

这样宿主负责"读人生"，Ventana 只负责"聊"。**做嵌入之前先把这条接口定下来**，
否则会写出互相依赖的两坨代码。

### 已为嵌入准备的（也仅此而已）

- 单文件、零依赖、无构建：拷一个目录就能跑
- 没有全局变量污染（除了 `VERSION` 等少量内部量），没有第三方 SDK
- 存储键集中成 `K_CFG/K_MSGS/K_PROMPT` 三个常量，换命名空间只改三行
- `buildMessages()` 是唯一的"发出去之前组装请求"的地方，接宿主的上下文从这里进

---

## 9. 无头 Chrome 里 rAF 不触发（2026-09-12 发现）

**发现过程**：测试里"流式过程中光标在闪"这条断言时好时坏，追下去发现根因不是断言写错，
而是**这个无头 Chrome 里 `document.hidden === true`、`requestAnimationFrame` 一次都不触发**。

**后果**：原来的 `appendDelta` 把绘制全压在 rAF 里 → 字都收到了，屏幕上一个字都不出，
只剩"正在输入"三点一直转。测试里表现为"文本长度 0 但 `_raw` 已经有值"。

**两条对策**：

1. **App 侧（真正的修复）**：`paintSoon()` —— rAF 与 120ms 兜底定时器谁先到谁画，
   画完取消另一个。`requestAnimationFrame(...)` 外面还包了 `try`：
   某些环境调用它就抛，抛出去的话 `el._paint` 永远不复位，那条气泡从此不再更新
   （表现同样是三点一直转）。真机上的对应场景：后台标签页、被遮住的窗口。
2. **测试环境**：起无头 Chrome 时加
   `--disable-backgrounding-occluded-windows --disable-renderer-backgrounding
   --disable-background-timer-throttling`，页面才会被当作可见、正常产帧。
   README 的启动命令里已经带上。

**顺带记一条**：页面不可见时浏览器还会把定时器节流到 ~1s 一次，所以兜底不是"立刻出字"，
而是"最终会出字、不会永远空着"。`mobile.mjs` 里的「rAF 失效时的兜底」一组
是把 `requestAnimationFrame` 打成空函数来测的 —— 比依赖环境状态可靠。

---

## 10. 演示模式是自动兜底，不是开关（2026-09-12 改）

作者反馈：「演示模式是无 API 连接时的默认模式，它不需要主动开启，
所以不要让它占据一个滑块板块」。另外一个并列的问题是：API 连接和系统提示词两块挨得太紧。

**改法**：

- 删掉「连接方式：演示模式 / 真实 API」这个滑块，以及 `cfg.demo` 这个存储字段。
- **改为算出来的状态**：`apiConfigured()` = 三项（Base URL / Key / 模型名）是否都非空。
  齐了走真实 API，缺一项走内置演示回复。这样不会出现"滑块停在演示模式但其实已配好"
  这种自相矛盾（旧设计真的有这个风险）。
- 顶上放一条**会变的状态横幅**：演示模式下明说"现在是演示模式、还缺哪几项、
  填好保存就会自动切过去"；配好了就显示"已连接 · <模型名>"。
- 加「清除连接」按钮 —— 它是**回到演示模式的唯一入口**，
  免得用户为了试演示模式去手动删 Key。人格不会被一起清掉。
- 两块之间：大间距（34px）+ 细分隔线 + 小节标题（「API 连接」/「系统提示词 · 决定它是谁」）。

**回归测试**（`mobile.mjs` 的「演示模式自动兜底」一组，20 项）：
界面上没有任何模式选择控件、演示模式下发 0 次网络请求、只填两项仍是演示模式、
三项齐了不点任何开关就走真实 API、清除连接后回到演示模式且人格保留、
两块之间的间距与分隔线存在。

---

## 11. 四个新功能怎么设计的（2026-09-12 加）

这一节记的是**为什么这么设计**，不是代码 —— 代码在 `app.js` 里，
上面第 2 节有地图。

### 资料库：为什么是"索引 + 按需读取"

用户的原话是「上传的文档可能字数会非常多，我希望系统可以自动把文档解析成一个 skill，
按需读取，这样不烧 token」。做法：

- 文档正文只存 localStorage，**不进提示词**；
- 提示词里只放一行索引：`- 《名字》（3200 字）：开头 60 字…`；
- 模型要正文时自己索取。**两条路都留着**：
  - 原生 tools（`read_doc`）—— 支持的端点最可靠
  - 文本协议 `[[读:名字]]` —— 端不支持 tools、或小模型不老实走 function calling 时的退路
  - 端点第一次拒绝 tools 时（`/tool|function|param/` 命中错误信息）会自动关掉 tools 重试一次，
    不让"不支持"变成"不能用"
- 用户可见：聊天里会多一行 `读了《…》`，读文档这件事是**看得见**的，不偷偷读

为什么不用向量检索：当前文档量是"几份到几十份"，按名字索取就够了。
真要检索式 RAG，接口点是 `docIndexText()` / `findDoc()`，换掉它们即可。

### 气泡菜单：为什么在气泡下面，为什么直接改存储

- 放气泡**下面**而不是悬浮：悬浮按钮在手机上要么挡字、要么要长按。常驻一行小字最直接。
- 按钮数量按角色区分：我方消息没有"重新生成"（没有"重新生成我"这种需求）。
- **菜单直接改存储**（`doDelete` 会 splice 会话里的消息并落盘），不是只改界面 ——
  否则刷新一下删掉的东西又回来了。
- 「重新生成」的语义是"从这条回答开始往后全丢，再问一次"，所以它天然支持
  "从断掉的地方接着来"。
- 生成过程中**不挂菜单**：挂了也能点，但点了会出错。

### 会话归档：为什么用"打标记"而不是"移动数据"

`archived: true` 只是标记，会话始终躺在同一个数组里。好处：
数据不动、迁移时不用担心丢东西、归档列表按 `createdAt` 排序也不受影响。
（当前抽屉只提供导出 / 删除 / 改名，没有「取消归档」入口；归档后想继续那个话题就开新会话。）
归档后**立刻开一个新会话**，不留"当前会话是已归档的"这种怪状态。

导出是纯文本（不引任何库），文件名用会话标题。删除有二次确认。

### 记忆馆：为什么只给 AI 写、不给用户建

用户的设计意图是「完全由 AI 按需读写」。所以记忆馆里**没有"新建"按钮** ——
只提供看、展开、复制、删。这样它不会退化成又一个需要人维护的表单。
写入路径和读文档同构：tools（`save_memory` / `read_memory`）+ 文本协议（`[[记:…]]` / `[[忆:…]]`）。

### 这一轮的四个真 bug（都是加功能时新引入的）

| bug | 症状 | 原因 |
|---|---|---|
| `showTyping` 永远不显示 | "正在输入"从未出现 | 先 `if (!typingEl) return` 才去创建元素 —— 判断写在了创建之前 |
| 记忆列表不重排 | 新写的记忆挂在列表末尾 | `addMemory` 排了，但"AI 刚写完"和"从存储读出来"是两条路径，渲染前没统一排 |
| 归档删除不刷新 | 删掉了但列表还在 | 删完只 `renderMemory()`，漏了 `renderArchives()` |
| 重新生成留下空壳 | 点了之后多出一条空回答 | 拆旧节点与 `currentAiMsg` 的指向没对上，见第 2 节第 3 条 |

### 演示节奏在测试里被调快（`window.__VENTANA_TEST_FAST`）

整套浏览器测试原本要跑 **55 秒**，贴着 60 秒的工具上限 —— 边缘会开始 flaky
（跑四次里有一次超时）。所以在 `demoStream` 里加了 `pacing()`：
页面上有 `window.__VENTANA_TEST_FAST` 时把所有停顿压到 4ms，测试启动时注入它。
生产环境没有这个开关，节奏就是 14–26ms/字、420–940ms/气泡间隔。

**一个副作用要知道**：个别断言是"对演示产生的每条气泡都检查一遍"，
所以节奏变快、固定等待没变时，被检查的气泡数会少一点 —— 断言的**总数**会浮动
（219～225）。这不是失败，但如果你看到总数变了，先想想是不是节奏或等待被改过。
想让总数稳定，就把那些断言改成"对已知的那几条气泡检查"，别依赖演示吐了几条。

### 测试夹具自身的三个坑（比 app 的 bug 更花时间）

1. **手写 SSE 时把 `\n` 写成了字面量**：出站串里是"反斜杠 + n"而不是换行，
   客户端按行切不出 `data:` 前缀，整个响应被静默忽略 ——
   看起来像"模型没输出任何东西"或"模型没提工具调用"，其实是夹具的错。
   **为此加了 `sseBody()` 助手并把 `SSE_FN` 注入页面**，别再手写。
2. **`SSE_FN`（页面侧助手）和 `sseBody`（Node 侧）不是一回事**：假 `fetch` 跑在浏览器里，
   Node 作用域的函数它看不到，直接用会 ReferenceError（然后请求静默失败）。
3. **假回答是毫秒级完成的**，所以"点完立刻断言 busy"必然会翻车。
   要断言的是可观察的结果（请求发出、角色序列、消息条数），不是瞬时状态。

---

## 11.5 气泡图标排的位置（2026-09-12 改，作者明确要求过）

作者的原话：「做成小型图标放在我说话的【气泡】和 AI 说完的【那段话】的【下面】！
**不要摆在右边**！」

改之前是文字按钮（复制 / 重新生成 / 删除），而且整排靠右。两个问题：
中文两个字比图标宽一倍多，三个并排把整行撑得比气泡还长，看起来就"挂在右边"。

现在的规则（**别再挪**）：

| 项 | 值 |
|---|---|
| 形态 | 内联 SVG，1.7px 描边、16px 见方（和顶栏那套图标同源） |
| 我方 | 贴气泡**右下角**（`right: -10px` 抵消最后一个按钮自己的右内边距） |
| 对方 | 贴**整段消息**左下角（`left: -10px` 同理） |
| 横向 | **永远一排**（`flex-wrap: nowrap`） |
| 点击区 | 36×30（图标本体 16px，靠 `padding: 0 10px` 调和） |
| 条数 | 对方 3 个（复制/重新生成/删除）；我方 2 个（没有"重新生成我"这种需求） |
| 开场白 | **不给图标**（它是引导文案，不是对话） |

### 第三个坑：一轮回答的图标挂在哪条气泡下面

一轮回复可能是**多条气泡**（演示模式连发、工具调用打断正文）。
一开始挂在 `replyEls[0]` 下面，结果图标夹在两段话**中间** ——
作者原话：「出现在了"你好，我是 Ventana…"句子的下方，而不是整段话的下方」。
现在由 `attachActionsToReply()` 统一挂到**最后一个有内容的气泡**下面，
所有气泡共享同一个 `_msgIndex`（它们本来就是同一条 assistant 记录）。

### 第四个坑：点「停止」的回答没有图标

`stopChat()` 是另一条结束路径，原来只 `finalize` 不挂图标 ——
表现是"按过停止的那条回答下面什么都没有"。现在两条路径都走
`attachActionsToReply()`。顺手修掉：停止时清空壳之后要**重新取一次**索引，
先算索引再删节点的话会留下一个没有图标的空气泡。

### 改这个位置时踩的两个结构坑

1. **`.acts` 不能做 `.row` 的直接子元素。** `.row` 是横排 flex，
   图标排会被当成"再开一列"，在 390px 屏上被挤成 **38px 宽**（三个图标叠在一起看不见）。
   → 现在结构是 `.row > .col > (.body + .acts)`，`.col` 竖排。
2. **`.row.me .col` 不能设 `align-items: flex-end`。** 那样图标排的 `margin-left: -10px`
   会被对齐计算吃掉，图标偏出气泡左缘 10px。→ 只让 `.body` 自己 `align-self: flex-end`，
   `.col` 用 `align-items: stretch` 让 `.acts` 的 `width:100%` 等于气泡宽度。

### 为什么用 `margin-left: -10px` 而不是给 `.col` 留内边距

为了让**图标本体**的左缘与气泡左缘齐平，整排要往左收 10px（等于按钮的左内边距）。
收出去的这 10px 会让点击区比 `.col` 宽，所以窄屏可能溢出 —— 因此
`mobile.mjs` 的「横向溢出」一组**在 320 / 390 / 900 三个宽度下**都断言了
"图标排都在视口内"和"图标都对齐气泡左缘"。

### 测试里两个假阳性（已修，别再写回去）

- 量"菜单在内容下方"时不能拿 `.body` 的 `top` 比：菜单挂了负外边距，空气泡那种高度
  里它会算成"在上面"。要拿**气泡/文字自己的 bottom** 比。
- 像素采样"对方回复没有底色"时，采样点不能太靠下 —— 会打到图标（图标是 `--text-2` 灰，
  同样"非背景"），误报过一次 145 的差异。要在**最后一行文字**那一带取样。

---

## 11.8 代码 review 抓到的 bug（2026-09-14，改动已合入）

测试 273 项全绿的情况下，人工 review + 定向探测还是抓到两个真 bug，
外加一个顺带发现的启动路径漏刷新（Bug 3）。都属于**测试没覆盖到的组合**，记下来当教训。

### Bug 1：模型名长了会压到顶栏那排按钮上

```
.chip 宽 172px（max-width: 44vw）   ← 外壳被正确约束
#modelTag 实际宽 257px              ← 里面的 span 没有任何约束
→ 文字右缘 295px，导出按钮从 286px 开始 → 压住 9px
```

根因：**`.chip` 上的 `overflow/ellipsis` 只管胶囊自己的盒子，管不到子元素**。
span 会照自己内容的宽度长，超出部分被父级的 `overflow: hidden` 裁掉后
"看起来没事"，但布局上它已经伸到按钮底下了（`elementFromPoint` 也证明按钮还能点到，
所以更隐蔽）。

修法：给 `#modelTag` 自己设 `min-width:0 + overflow:hidden + text-overflow:ellipsis`。
回归测试在 **320/390/414 三个宽度 × 四个模型名**下断言"文字右缘 ≤ 第一个按钮的左边"。

> 教训：给容器设截断样式不等于给文字设截断。凡是 flex 里放"可能很长的文本 + 图标"，
> 截断都要设在**文字那一层**。

### Bug 2：把资料库删空后，全量同步的通知整条被吃掉

```js
var di = docIndexText();            // 库空 → 返回 ''
if (di) { if (sync.docs) { …全量… } }   // ← 空串直接跳过整段
if (sync.prompt || sync.docs) clearSync();  // ← 标记照样被清掉
```

用户把最后一份文档删掉，本意是"告诉模型没资料了"，结果：
**请求里连 system 消息都没有**（实测 `有 system: false`），标记也被清了 ——
模型还按旧索引以为有资料可用。

修法两处：
1. 库空但标记了 `sync.docs` 时，**明确发一句**「资料库现在是空的，之前那些文档已全部删除，
   不要再引用」；
2. 标记不再在**构建阶段**清，改成请求**真的成功之后**才清（`markSyncSent()` 挂在
   `streamChat` 的 `.then` 第一行）。这样那一轮失败（断网/401）时标记留着，下轮继续全量下发。

顺带：`noteSourceChange()` 现在在**演示模式下不记**标记 —— 那时候根本不会发请求，
记了只会留个标记，等用户接上 API 后第一条消息突然全量下发，让人意外。

### Bug 3（修 1、2 时顺带发现的）：重开 App 后开场白变回"演示模式"版

`refreshGreeting()` 是在做"开场白跟着连接状态换文本"时加的，但它**只在
「保存连接」和「清除连接」时被调用**，漏了**启动**这条路径。
于是接上 API 用一阵子、重开 App，开场白又变回"现在还是演示模式…点右上角那枚齿轮" ——
看起来像连接没生效。修法：启动时（`restoreLog()` 之前）也调一次 `refreshGreeting()`。

> 教训：这类"派生文本 + 落盘成消息"的设计，**每一处状态变更入口都要刷新它**。
> 现在有三处：保存连接、清除连接、启动。再加状态维度时先数一遍入口。

### 同时清掉的死代码

`switchTo(convId)` 没了调用者 —— 那个「取消归档」按钮被移除后，它是唯一入口
（归档现在是单向的，见第 11.7 节）。留着会误导接手的人，已删。

---

## 15. 跨浏览器兼容性 —— 两个问题已修（v0.25）

作者在国产浏览器上遇到两个排版问题（附截图）。**根因全部用 DOM 实测确认过**，
不是猜的。两个都属于「布局依赖了旧内核会算错的东西」。

### Bug C：夸克（夜间）—— 用户气泡下方一大片空白 ✅ 已修

截图实测（DPR=3）：用户气泡高 114px，而它所在的 `.row.me .col` 高 211px
—— **多出来的那一块整块是空的**。同一条消息在浅色那张图上只有 114px。

根因：`.row .col` 用了 **`flex: 1 1 auto`**（grow）。旧版 WebKit 内核对
「flex-grow 的 item + 内部 `align-self`」这种组合的高度计算有差异，
会把 `.col` 的高度算成**整行高度**而不是内容高度，于是气泡下面那段
（给图标预留的 `padding-bottom: 38px`）被撑成了可见空白。

修法：`.row .col { flex: 0 1 auto }` —— 取消 grow。宽度上不需要它：
单行里只有 `.col` 一个 flex item，`flex-basis:auto` 已经会占满。

> CDP 实测确认：改后 `.col` 高度 = 内容高度，只有真挂了图标的那两行多出 38px。

### Bug D：OPPO 内置浏览器 —— 顶栏上方空一块、顶栏被压矮 ✅ 已修

截图实测：屏幕顶到顶栏内容有 **107 CSS px 空白**（同一台手机的夜间模式只有 19px），
而顶栏内容高实测只有 **41 CSS px**（CSS 里写的是 `min-height: 56px`）。

两个原因叠在一起：

1. **`min-height` 在旧内核里不含 padding** —— 只写 `min-height: 56px`，
   顶栏被压矮到「内容高 + padding」（实测 41px）。
2. **`env(safe-area-inset-top)` 在国产 Android 浏览器上会返回偏大的值**，
   而浏览器可能**已经让开过状态栏**了 —— 于是又加一份 padding，
   在顶栏上方撑出一块空白（浅色主题下就是那块白）。

修法三层：

- `min-height: calc(var(--sat) + 46px)` —— 下限跟着安全区走，且**含 padding**；
- `--sat: 0px` → `--sat: min(env(safe-area-inset-top, 0px), 30px)` ——
  先给回退值（老内核丢掉 `min()+env()` 也不会塌），再给 30px 上限；
- **`applySafeArea()` 实测纠正**：`visualViewport.offsetTop > 4` 说明
  浏览器已经让开了状态栏，这时把 `--sat` 置 0（并打 `data-safe-top="reserved"`）；
  否则才用 `env()` 的值。**这才是真正解决「重复计算」的一层** ——
  单靠 CSS 封顶挡不住 32px 这种「不大不小」的异常值。

### 顺带查出的第三个问题：手机上拿不到新 CSS ⚠️

排查时发现：`index.html` 引用的是裸的 `styles.css`（没有 `?v=`），
而 `sw.js` 对静态资源是**缓存优先 + `ignoreSearch: true`**（连版本号都忽略）。
只要有中间层按 URL 缓存，**改了 CSS 手机上就永远看不到** ——
「改完刷新还是旧的」就是这么来的。

修法：

- `sw.js` 里把「改了必须生效」的资源（html / css / js）改成 **网络优先**，
  离线才落回缓存（图标仍缓存优先，它们基本不变）；
- 引用加 `?v=`，且**由 `serve.py` 自动替换**（`index.html` 里写 `?v=__V__`，
  服务端从 `app.js` 读 `VERSION` 填进去）—— 版本号只在 app.js 维护一处。

> ⚠️ 注意：通过局域网 IP 访问时 `isSecureContext` 为 false，
> 有些浏览器**根本不注册 Service Worker**（本项目实测 `navigator.serviceWorker`
> 为 `undefined`）。所以那条缓存优先策略只在 PWA/HTTPS 下生效；
> 普通 HTTP 访问时挡路的是浏览器自身的 HTTP 缓存 ——
> 这也是为什么加了 `?v=` 之后，**彻底关掉页面重开**才最稳。

### 通用教训（这一节值得反复读）

1. **`flex-grow` + 内部 `align-self` 是重灾区**。想让容器按内容撑高，
   就别给它 grow；宽度用 `flex-basis: auto` 通常已经够。
2. **`min-height` 在旧内核里不含 padding**。想要「最低总高」，
   写 `min-height` 时要把 padding 算进去。
3. **`env(safe-area-inset-*)` 在 Android 上不可全信**。要封顶、要给回退值；
   能用 `visualViewport.offsetTop` 实测纠正，就不要只靠 CSS。
4. **「改了看不到」先怀疑缓存路径**，别急着怀疑自己改错了 ——
   SW、HTTP 缓存、代理都可能挡住（见上面第三个问题）。
5. **截图报告也会出错**：这次两张图被误标成「图1=夸克夜间、图2=OPPO」，
   实际按像素看是「浅色=夸克、深色=OPPO」——**主题和文件名是反的**。
   凡是与实测不一致的描述，以实测为准。

---

## 16. 公开仓库与 GitHub Pages（2026-09-16 建立）

- **仓库**：`Sapphirella/ventana`（public）
- **线上**：https://sapphirella.github.io/ventana/
- **Pages 配置**：`main` 分支的 `/` 根目录（legacy 构建）
- 仓库描述：`Ventana —— AI 虚拟角色聊天 App（PWA，手机端优先）`

### 仓库里放什么

**只放 `ventana/` 里的内容，且放在仓库根目录**（所以 `index.html` 在 `/ventana` 仓库的根上）。
本地工作区的仓库根**不是** Ventana —— 除 `ventana/` 外还有若干**私人素材目录**，
一律不发布。发布前请务必确认：

```bash
# 检查待发布文件里有没有私人素材痕迹（应全部为 0）
for kw in <私人关键词...>; do grep -rl "$kw" ventana/*.md ventana/*.py ventana/*.js; done
```

已知会被排除的：`ventana/.dumate/`（本地调试截图，**里面有聊天内容，绝对不能发**）、
`.DS_Store`。

### 怎么推（⚠️ 这条网络下 git 协议不通）

**本机所在校园网封了 `github.com:443`** —— `git push` / `git ls-remote` 会直接超时。
但 `api.github.com` 和 `codeload.github.com` 是通的，所以用 **Git Data API** 提交：

```bash
# 脚本在 ventana/.dumate/ghpush.py（不进公开仓库）
GH_TOKEN=xxx python3 ventana/.dumate/ghpush.py --dry-run   # 先看差什么
GH_TOKEN=xxx python3 ventana/.dumate/ghpush.py             # 真提交
```

它做的事：读远端 HEAD → 用 blob SHA 逐文件比对（内容相同就不重传）→
建 tree（**不传 base_tree，即"结果恰好是这些文件"**）→ 建 commit → 更新 ref。
所以**不会误删远端文件**，也能明确报出"远端多出来的文件"。

> 如果哪天换到能连 github.com 的网络，`git remote add origin ... && git push` 更省事。
> 但注意：**远端是独立的一份历史**（用 API 提交时没有本地增量历史），
> 和本地工作区的 git 不是同一条线 —— 别指望两边提交能对上。

### 版本号在静态托管下怎么生效

`index.html` 里写的是 `styles.css?v=__V__`（占位符），有两层替换：

1. **本地/开发**：`serve.py` 读 `app.js` 的 `VERSION` 替换掉；
2. **静态托管（Pages）**：`serve.py` 不参与，由 **`sw.js` 在返回 index.html 时替换**
   （`withVersion()`）。所以在 Pages 上也能拿到 `?v=v0.26` 而不是字面量 `__V__`。

另外仓库根放了 **`.nojekyll`** —— 不让 GitHub Pages 用 Jekyll 处理（否则
下划线开头的目录 `_vendor/` 会被忽略）。

### Pages 上线后要检查什么

- `isSecureContext === true`（HTTPS）→ **Service Worker 才会注册**，
  这也是"手机上改完看不到新样式"的根治点（HTTP 局域网访问时 SW 根本不存在）
- 顶栏高度 ≈ 61 CSS px、`--sat` 有上限、`.row .col` 的 flex 不是 grow
- 引用里是 `?v=<真实版本>` 而不是 `?v=__V__`

---

以下几件事接手时**不要自己拍板**，先问：

1. **配色方向** —— 现在刻意是黑白，作者说过"后期再慢慢配置 css"。
2. **人格要不要变成多份** —— 现在是"一份，整段替换"。如果以后想有"平时 / 深夜"
   两套性格，或者想让人格文件按情境自动加载，那是数据层改动，越晚越贵。
3. **桌面端要不要支持** —— 当前只有移动端断点（≥720px 只是把消息区限宽居中）；
   v1 做过 ≥960px 三栏，可以参考。
4. **要并进的那个博客系统** —— 第 8 节列的三个待定项（域名 / Key 放哪 / 怎么喂文章）。
   这三件事定了之后，嵌入方案才好选。
5. **是否要走真实的分发**（域名 / 托管 / 上架），这会影响 PWA 的 scope 与图标策略。
