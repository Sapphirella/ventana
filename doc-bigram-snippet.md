# Ventana 大文档按需截取：bigram 打分逻辑说明

> 说明：本文件描述 `app.js` 中 `docSnippet()` / `docBigrams()` / `splitDocBlocks()` 的实现逻辑与设计意图。实现以代码为准，本文档解释"为什么这么做"与"边界行为"。

## 1. 背景与目标

用户上传到资料库的文档可能有几万字。如果每一轮对话都把整份文档发给模型：

- token 烧得很快；
- 大多数轮次根本用不到文档内容；
- 上下文被无关文字挤占，反而影响回复质量。

所以 Ventana 的约定是：**系统提示词里只放文档索引**（标题 + 开头 60 字），模型需要时通过工具调用 `read_doc` 或文本协议 `[[读:文档名]]` 索取。索取时**不整篇给**，而是按需截取：

- 文档 ≤ 6000 字 → 直接给全文（小文档，整篇也烧不了多少 token）；
- 文档 > 6000 字 → 按"与当前话题的相关性"截取**一段连续区间**，总长不超过 6000 字。

## 2. 整体流程

```
模型索取（read_doc / [[读:]]）→ runAction('读')
   → doc.text.length > DOC_SNIPPET_MAX(6000) ?
        └─ 是 → docSnippet(doc)
               ├─ 构造话题上下文（最近 2 条 user 消息 + 文档名）
               ├─ 生成话题 bigram 集合 target
               ├─ 把文档切成块（splitDocBlocks）
               ├─ 逐块打分，取命中最多的块为锚点
               ├─ 无命中 → 返回开头 6000 字，matched=false，如实告诉模型
               └─ 有命中 → 从锚点向两侧扩展连续区间至 ≤6000 字
        └─ 否 → 返回全文
```

调用方（`runAction('读')`）统一在结果文本前加一段说明："《文档名》共 N 字，以下是与当前话题最相关的部分（最多 6000 字，按需截取，不会整篇输出）"；`matched=false` 时如实改为"没找到与当前话题直接相关的部分，以下先给开头部分"。同时生成一条系统事件 **"读了《文档名》· 实际给了多少/总字数（按需截取）"**，让用户一眼看清这是按需取段、不是整篇读取。

## 3. 字符 bigram：`docBigrams(text)`

### 3.1 定义

一个**字符 bigram** 是文本里相邻两个字符组成的二元组。例如文本 `"鲸鱼吃鱼"` 产生的 bigram 集合为：

```
鲸鱼, 鱼吃, 吃鱼
```

### 3.2 构造步骤

1. **清洗**：用正则 `/[^A-Za-z0-9\u4e00-\u9fa5]/g` 去掉所有非中英文字母、非数字的字符（标点、空格、换行、emoji 全部剔除）。
2. **小写化**：英文字母统一转小写，让 `"Cat"` 与 `"cat"` 视为同一 bigram。
3. **滑窗**：对清洗后的字符串按步长 1 滑动取长度为 2 的子串，存入集合（`Set` 语义，去重）。

### 3.3 为什么用 bigram

- **零依赖、确定性**：不引入分词库或外部服务，同样的输入永远得到同样的输出，可测试、可复现。
- **抗错能力强**：跟全词匹配相比，bigram 只要求"两个字符连续出现"，对同义词、近义词、口语表达都更宽容；词汇在话题里反复出现时，其内部字符对会大概率重叠。
- **中文友好**：中文字与字之间没有空格，bigram 不需要分词即可捕捉相邻字之间的搭配关系（如"鲸鱼""哺乳""幼崽"各成一组）。

### 3.4 应用场景

- 话题上下文 → `docBigrams(ctx + ' ' + doc.name)`，得到"目标 bigram 集合"；
- 文档每个候选块 → `docBigrams(块文本)`，得到该块的 bigram 集合。

## 4. 话题上下文

`docSnippet()` 这样取"当前话题"：

```js
var ctx = msgs()  // 当前会话全部消息
  .filter(m => m.role === 'user' && m.content)   // 只看用户说的话
  .slice(-2)                                     // 最近 2 条
  .map(m => m.content).join(' ');
var target = docBigrams(ctx + ' ' + (doc.name || ''));
```

- **只取最近 2 条 user 消息**：对话相关性主要来自就近上下文；取太长会把旧话题的词汇混进来，且每轮都要算，成本更高。
- **拼上文档名**：模型通常按名字索取文档（如 `[[读:海洋生物大全]]`），文档名本身是极强的相关性信号，必须参与打分。

## 5. 文档分块：`splitDocBlocks(text)`

打分需要"哪个位置最相关"，所以先把整篇文档切成**候选块（block）**：

1. 按 `\n{2,}`（空行）切成段落；
2. 段落长度 ≤ 400 字 → 整体作为一个块；
3. 段落 > 400 字 → 按句号、问号、感叹号、分号等句界切成句子（正则 `/[^。！？.!?；;]+[。！？.!?；;]?/g`）逐句成块；
4. **超长句**（如无标点的长文本）切成 ≤ 1200 字的连续块，`while` 循环逐段 `slice(0, 1200)` 切出，**绝不丢字**（这是 v0.14 修掉的一个真 bug：早期实现 `slice(0,1200)` 只取一次，会把超长段直接截断丢尾）。

块的粒度同时服务于两个目标：

- 块内文字"话题一致"（句子/短段落），打分才有意义；
- 块尺寸适中，扩展区间时步长可控。

## 6. 打分与命中判定

对每一个块：

```js
var bg = docBigrams(块文本), hit = 0;
for (var k = 0; k < targetKeys.length; k++)
  if (bg[targetKeys[k]]) hit++;   // 话题里每个 bigram 在块中出现一次就 +1
if (!best || hit > best.hit) best = { i: 块下标, len: 块长度, hit: hit };
```

- `hit` = 话题 bigram 集合中，有多少个在该块文本里出现（**对 target 的键计数**，不是块自身 bigram 总数，避免长块天然高分）；
- 遍历全部块后，`hit` 最大的块作为**锚点**；
- **平手取第一个**（遍历顺序靠前的块）。

### 6.1 无命中兜底

`best` 不存在（文档没有任何块）或 `best.hit === 0`（没有任何块与话题产生共同 bigram）时：

```js
return { text: text.slice(0, DOC_SNIPPET_MAX), matched: false };
```

- 返回**文档开头 6000 字**（开头通常是总纲/摘要，比随机位置更稳妥）；
- `matched=false` 让调用方如实告诉模型"没找到与当前话题直接相关的部分，先给开头部分"，模型不会误以为截取段就是最相关内容。

## 7. 连续区间扩展

命中单个块可能不足 6000 字，需要**向两侧扩展**，扩展出的必须是**一段连续区间**，保证文档连贯性：

```js
var lo = best.i, hi = best.i, total = best.len;
while (lo > 0 || hi + 1 < blocks.length) {
  var backLen = lo > 0 ? blocks[lo-1].text.length : Infinity;
  var fwdLen = hi+1 < blocks.length ? blocks[hi+1].text.length : Infinity;
  // 两侧都可取时，先吃较短的一侧（让区间尽量对称、靠近锚点）
  var takeBack = backLen === Infinity ? false
                 : fwdLen === Infinity ? true
                 : backLen <= fwdLen;
  var addLen = takeBack ? backLen : fwdLen;
  if (total + addLen > DOC_SNIPPET_MAX) break;   // 装不下就整体停止
  total += addLen;
  if (takeBack) lo--; else hi++;
}
return blocks.slice(lo, hi + 1).join('\n\n');
```

关键设计：

- **吃短侧**：每次取两侧中较短的那个块，让扩展窗口尽量贴近锚点，而不是把一侧的长块硬塞进去把另一侧挤掉；
- **装不下即停**：`total + addLen > 6000` 直接 `break`，两侧都停。若较短一侧都装不下，另一侧必然更长更装不下，所以不存在"这一侧装不下、换另一侧试试"的路径——这也是 v0.14 修掉的第二个真 bug（早期版本在单侧 blocked 时死循环，且块集合拼接会夹带文档尾部）；
- **结果恒 ≤ 6000 字**（加上 `\n\n` 连接符后仍接近上限，实际返回长度 ≈ `total + 连接符`，测试以 ≤6500 为容差断言）；
- **始终保持连续区间**：`blocks.slice(lo, hi+1)` 是原始顺序的连续子序列，不会把文档尾部无关内容拼进来。

## 8. 边界行为汇总

| 场景 | 行为 | matched |
| --- | --- | --- |
| 文档 ≤ 6000 字 | 直接返回全文 | `true` |
| 文档 > 6000 字、有块命中话题 | 锚点块 + 向两侧扩展的连续区间（≤6000） | `true` |
| 文档 > 6000 字、无任何块命中 | 返回开头 6000 字 | `false` |
| 文档正文为空 | `text` 为空串，返回 `{ text: '', matched: true }`（≤6000 分支） | `true` |

配套的不变量：

- 任何一次索取返回的截取内容**至多 ≈ 6000 字**，绝不整篇带出（唯一全文出口是 `ventana.sync.docs` 置位后"资料库刚刚更新"的首轮全量下发，属于有意的设计，不参与日常对话）；
- 系统事件用"给了多少/总字数（按需截取）"如实呈现，不显示容易让人误以为"整篇被读"的裸总字数；
- `read_doc` 工具的描述明确写明"长文档会按需截取，一次最多给 6000 字，不会整篇输出，读不完整是正常的"，从模型侧消除"索取=拿全文"的错误预期。

## 9. 相关文件与测试

- 实现：`app.js` 的 `docSnippet` / `docBigrams` / `splitDocBlocks`；调用方 `runAction('读')`。
- 工具定义：`app.js` 的 `TOOLS` 中 `read_doc`；文本协议：`[[读:文档名]]`（`MARK_RE`）。
- 测试：`tests/mobile.mjs` 的「大文档按需截取」组（文本协议路径）与「原生工具调用 · 两万字大文档」组（工具路径），断言：
  - 返回内容含目标话题关键词、不含文档尾部独有标志；
  - 长度 ≤ 6500（6000 上限 + 连接符容差）；
  - 系统事件匹配 `读了《…》· N/M 字（按需截取）`，且不再出现"（N 字）"的整篇误导。